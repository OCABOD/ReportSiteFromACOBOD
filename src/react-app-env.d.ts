/// <reference types="react-scripts" />

interface IJSWindow {
	google: any;
}

interface ISidebarButton {
	buttonText: string;
	path: string;
}
interface IBonusesButton {
	buttonText: string;
	path: string;
}